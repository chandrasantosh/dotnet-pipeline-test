name: .NET CI Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '6.0.x'

    - name: Restore dependencies
      run: dotnet restore ./sample-unit-testing-using-dotnet-test/*.csproj

    - name: Build
      run: dotnet build ./sample-unit-testing-using-dotnet-test/*.csproj --configuration Release --no-restore

    - name: Test
      run: dotnet test ./sample-unit-testing-using-dotnet-test/*.csproj --logger "trx"

    - name: TODO Deployment
      run: echo "TODO – Deployment"
